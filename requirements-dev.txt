# Development / test dependencies. NOT needed to run the app (Streamlit Cloud
# only installs requirements.txt). Install locally with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
